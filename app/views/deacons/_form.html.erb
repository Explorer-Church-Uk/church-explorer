<%= form_with(model: deacon) do |form| %>
  <% if deacon.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(deacon.errors.count, "error") %> prohibited this deacon from being saved:</h2>

      <ul>
        <% deacon.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :member_id, style: "display: block" %>
    <%= form.text_field :member_id %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
