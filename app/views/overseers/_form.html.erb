<%= form_with(model: overseer) do |form| %>
  <% if overseer.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(overseer.errors.count, "error") %> prohibited this overseer from being saved:</h2>

      <ul>
        <% overseer.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :user_id, style: "display: block" %>
    <%= form.text_field :user_id %>
  </div>

  <div>
    <%= form.label :bio, style: "display: block" %>
    <%= form.text_field :bio %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
